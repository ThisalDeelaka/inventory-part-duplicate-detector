import { useEffect,useState } from 'react';import{Link,useParams}from'react-router-dom';import{api}from'../api/client'
export default function Warnings(){const{id}=useParams();const[items,setItems]=useState([]);useEffect(()=>{api.get(`/api/scans/${id}/warnings`).then(setItems)},[id]);return <><header><div><p className="eyebrow">Data quality</p><h1>Scan warnings</h1><p>Warnings preserve scan continuity while making uncertainty visible.</p></div><Link className="button secondary" to={`/scans/${id}`}>Back to results</Link></header><section className="panel">{items.length?items.map(w=><div className="warning row" key={w.id}><div><b>{w.warning_type.replaceAll('_',' ')}</b><small>{w.message}</small></div></div>):<p className="empty">No data-quality warnings were recorded.</p>}</section></>}
