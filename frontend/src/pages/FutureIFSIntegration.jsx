export default function FutureIFSIntegration(){return <><header><div><p className="eyebrow">Documented boundary</p><h1>Future IFS integration</h1><p>The current demo uses CSV exports with IFS-compatible field names only.</p></div></header><section className="panel prose"><h2>Current scope</h2><p>No IFS Cloud API, Projection, OAuth, direct table, lobby, or event-action integration is implemented. The detection engine remains transport-independent.</p><h2>Potential preventive flow</h2><ol><li>A user creates or edits an inventory part in a supported IFS workflow.</li><li>A future adapter sends canonical part data to the duplicate-check API.</li><li>The engine returns ranked possible duplicates with explanations.</li><li>IFS displays a warning and keeps the final decision with the user.</li></ol><h2>Production evolution</h2><p>Deploy the stateless API on Kubernetes, replace SQLite with PostgreSQL, add an approved IFS Cloud API/Projection adapter, and use reviewed feedback for supervised calibration. Detection must remain advisory; automatic merging is outside the intended boundary.</p></section></>}
